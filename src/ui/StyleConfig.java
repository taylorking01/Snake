package ui;

import javafx.scene.paint.Color;

public class StyleConfig {

    // Define common colors used across the application
    public static final Color BACKGROUND_COLOR = Color.BLACK;
    public static final Color TEXT_COLOR = Color.LIMEGREEN;
    public static final Color BUTTON_TEXT_COLOR = Color.WHITE;
    public static final Color BUTTON_BACKGROUND_COLOR = Color.DARKGRAY;

    // Define other styles or methods for styling elements
}
